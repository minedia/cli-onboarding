# outline.md — 講師用 詳細タイムテーブル

> 60分・オンライン・ハンズオン。Windows 9割 / Mac 1割想定。
> このドキュメントは**講師の手元資料**。受講者用ハンズオン手順書は `handson/` に別途用意する。
>
> **シェルは bash 系で完全統一**：Windows = Git Bash、Mac = zsh。
> 入門範囲では方言差ほぼゼロ。PowerShell は使わない（理由：方言を覚える手間が増える＆AIエージェントが扱うのも bash 系が標準）。

---

## 0. 講座メッセージ（最重要）

### 一行で言うと

> **AIエージェント時代に、CLIを知る人と知らない人の生産性は16倍以上違う。**
> その差は毎月毎月、複利で広がっていく。

### なぜ「今」なのか

過去：CLI は "エンジニアの道具"。一般職には不要だった。
**今：CLI は "AIに仕事を任せる全員の道具"。**

AIエージェント（Claude Code、Cursor、各種MCP）は、GUIではなくテキスト＝コマンドラインを介して動く。AIに業務を委任しようとすると、必ずどこかで「ターミナル」にぶつかる。

- AIに「このCSVを整形して」と頼む → AIはスクリプトを書く → **動かすのはあなた（CLI）**
- AIに「画像を一括変換して」と頼む → AIはコマンドを返す → **コピペして実行するのはあなた（CLI）**
- Claude Codeを契約した → **起動・操作・ファイル指定が全部CLI**

「コマンドプロンプトに貼って実行してください」で止まる人と、自分で改造して定期実行まで持っていく人の差。これがAI時代の新しい "PCリテラシー" になっている。

### 格差の具体イメージ（オープニングでスライドに出す数字）

| | CLIを使えない人 | CLIを使える人 |
|---|---|---|
| Claude Codeで自動化できる業務 | 月3時間 | 月50時間 |
| 100ファイルのリネーム | 2時間（手作業） | 3秒（1コマンド） |
| AIスクリプトをもらった時 | 「動かし方がわからない」 | 即実行・改造・定期化 |
| 同じツール、同じ料金、同じ給料 | 生産性 ×1 | 生産性 **×16** |

> ※「月50時間」「16倍」は感覚値。受講者が"自分ごと"として体感できる数字感を狙う。実数は講演時に「私（CTO）の体感では…」と前置きする。

### さらに：Claude Code の "裏側" もまったく違う

AIエージェントは、あなたの環境次第で**裏側でやっていることが根本的に変わる**。
同じ「このプロジェクトから TODO を全部探して」という依頼でも：

| | CLI環境（あなたがCLIを使える） | GUI環境（CLIが分からない） |
|---|---|---|
| AI内部の動作 | `grep -r "TODO" .` を実行して結果を読む | デスクトップのスクショ撮影 → 画像解析 → 座標特定 → クリック → またスクショ → ... の延々と繰り返し |
| 所要時間 | **0.5 秒** | 数分〜十数分 |
| トークン消費 | 約 1,000 トークン | **数十万〜数百万トークン**（数百倍） |
| 失敗時の回復 | コマンドを直して即再実行 | 何が起きたか不明・最初からやり直し |
| あなたが理解できるか | コマンドが読めるから追える | ブラックボックス |

つまり、**CLIが使える環境を持っていることそれ自体が、AIの "燃費" と "賢さ" を桁違いに上げる**。
あなたのPCがCLI環境なら、AIは省エネで賢く動く。GUIしかなければ、同じAIが数百倍のコストで、それでも手探りで動く。

> これがAI時代に「CLIを知っている人ほど、AIに任せられる量が指数関数的に増える」という構造的な理由。

### メタファ（オープニングで使う）

> **AIエージェントは「英語しか喋れない、超優秀なアシスタント」。CLIはその英語そのもの。**
> 英語が読み書きできない人は、せっかくの優秀な助手に1ミリも仕事を頼めない。
> 今日の60分は、その「英語」のいちばん基礎を体に入れる時間。

---

## 1. タイムテーブル（60分）

| 時刻 | 分 | セクション | ハンズオン | 講師の主目的 |
|------|----|----|----|----|
| 0:00 | 6  | 0. オープニング：格差の話＋AIの裏側 |  | "受講しない損"を腹落ちさせる |
| 0:06 | 5  | 1. CLIとGUIの違い＋text vs binary | ① ターミナルを開く | 2つの比較軸を入れる、黒い画面の心理的ハードル除去 |
| 0:11 | 10 | 2. シェルとコマンドの基礎＋ショートカット | ② 基本コマンド5つ＋`--help`／Ctrl+C実演 | コマンドの"文法"とショートカット |
| 0:21 | 13 | 3. ファイル・ディレクトリ操作（+`tree`+GUI⇄CLI） | ③ 作業フォルダで CRUD | カレント / パスの概念を体感 |
| 0:34 | 5  | 4. 探す：`find` と `grep` | ④ ファイルを探す・中身を探す | CLIの真骨頂、AIエージェントの心臓部 |
| 0:39 | 3  | 5. 環境変数 | ⑤ `PATH` を覗く | "なぜAPIキーは環境変数か"を理解 |
| 0:42 | 9  | 6. Claude Code を自分で動かす ★ゴール | ⑥ `claude` 起動 → 自然言語で頼む | "AIに業務を任せる"を体感 |
| 0:51 | 2  | 7. 次に効くコマンド集 |  | "今日の続きはこれ"を見せる |
| 0:53 | 4  | 8. 応用デモ（見せるだけ） |  | "ここまでできる"を見せる |
| 0:57 | 3  | 9. クロージング・質疑 |  | 次のアクションへ送り出す |

合計 60分。各セクションは**ハンズオン優先**、説明は最小限。

> ※ `find` / `grep` をハンズオンに追加した理由：
> オープニングで紹介する「Claude Code の裏側で grep が走っている」話と本編が繋がるため、必ず一度は自分の手で叩いておく必要がある。「AIが裏でやっていることを、自分でもできる」体感が一番効く。

---

## 2. セクション詳細

### 0. オープニング（6分）

**話すポイント**
1. （30秒）この講座を受けると、Claude CodeやPythonスクリプトが"自分で動かせる人"になる
2. （1.5分）格差の話：表（CLI使える/使えない）をスライドで見せる
3. （1.5分）**Claude Codeの "裏側" 比較**：CLI環境 vs GUI環境でトークン消費が数百倍違う
4. （1分）「英語しか喋らない優秀なアシスタント」のメタファ
5. （30秒）今日のゴール：自分で書いたプログラムをターミナルから実行できるようになる
6. （1分）ハンズオン形式の説明、トラブル時の対応（チャットで質問）

**避けるべき脱線**
- "CLI vs GUI 論争" → 価値観の話に持ち込まない。「使い分け」が結論
- 「シェルって何種類あるか」の話 → 今日は触れない（bash / zsh / fish の違いは後日）

**FAQ想定**
- Q「私はエンジニアじゃないので関係ない？」→ A「むしろエンジニアじゃない人ほど、AIで仕事を任せる量が増えるので効きます」
- Q「Macしかないんですが？」→ A「むしろ Mac の方が標準でターミナルが入ってて楽です。Windows ユーザーは Git Bash を入れて、Mac と同じ環境にしてから参加してもらってます」

---

### 1. CLIとGUIの違い＋text vs binary（5分）★ハンズオン①

**話すポイント**
- GUI = 指差し注文（メニューを見て選ぶ）／CLI = 言葉で注文（「ホットコーヒー一つ」）
- 自動化・大量処理・AIとの相性は CLI が圧倒的
- **もう1つの比較軸：text vs binary**（30秒で触れる）
  - CLI vs GUI が「操作方法」の比較なら、text vs binary は「保存形式」の比較
  - text系（.md / .csv / .json）: AIが直接編集、Gitで差分、CLIで処理OK
  - binary系（.docx / .xlsx / .pptx）: 専用アプリ必須、AI と相性悪い
  - 「**同じ内容なら、可能な限り text 形式を選ぶ**」が今日のもう一つのテイクアウェイ
  - 例：Word議事録 → Markdown、Excel管理表 → CSV、PowerPoint → Marp（このスライド自身が実例）

**ハンズオン①：ターミナルを開く**
- Windows: スタートメニュー → "Git Bash" と入力 → Enter（Git for Windows をインストール済みの前提）
- Mac: Spotlight (`Cmd+Space`) → "ターミナル" と入力 → Enter
- 全員で「`echo Hello`」と打って Enter → 反応を確認

**避けるべき脱線**
- PowerShell との違い → 「使わないので説明不要」で済ます
- WSL のインストール手順 → 今日は Git Bash で十分（WSL は応用編で名前だけ紹介）
- カスタムテーマ・色設定 → 「便利だけど後で」

**FAQ想定**
- Q「PowerShell や コマンドプロンプト (cmd) ではダメ？」→ A「動きますが、コマンドの書き方が違います。今日は Git Bash で統一しています。理由：Mac と同じコマンドで講義できるから」
- Q「Git Bash って何？」→ A「Git for Windows に同梱されてる bash シェル環境。Windows 上で Linux と同じコマンドが動きます」

---

### 2. シェルとコマンドの基礎（9分）★ハンズオン②

**話すポイント**

**(1) シェルとは（30秒）**
- シェル = 「あなた」と「PC」の間に立つ通訳
- あなたが日本語/コマンドで指示 → シェルが翻訳 → PCが動く

**(2) コマンドのフォーマット — これが全ての基本（2分）**

> **CLIで打つもの = "スペースで区切った単語の列"。それ以上でも以下でもない。**

ルールはたった2つ：

1. **一番左の単語 = コマンド名**（何をするか）
2. **それより右の単語 = 全部そのコマンドへの "追加指示"（オプション / 引数）**

```
ls   -la   /Users
└┬┘  └─┬─┘  └─┬──┘
 │     │     └── 「どこの」を指定する追加指示
 │     └──────── 「どう表示するか」を指定する追加指示（- や -- で始まる）
 └────────────── コマンド名（一覧表示せよ）
```

ポイント：
- スペースで区切る。タブやカンマではない
- 右側は何個並べてもOK。順序もコマンドによる
- 「右側に何が書けるか」はコマンドごとに違う

**(3) `--help` の慣習（1分）**

> **「このコマンド、何ができるんだっけ？」と思ったら、まず `--help` を付ける。**

```
ls --help
git --help
claude --help
```

ほぼ全てのコマンドが、慣習的に `--help` で使い方を表示してくれる。
**辞書を引くようにコマンドを学べる**。これを知っているだけで、未知のコマンドが怖くなくなる。

- Mac / Git Bash では `man ls` も使える（詳しいマニュアル）
- 終了は `q` キー

**ハンズオン②：基本コマンド5つを叩く + `--help` を試す**

| コマンド | 意味 | 試す例 |
|---|---|---|
| `pwd` | 今いる場所を表示 | `pwd` |
| `ls` | 中身を一覧 | `ls`、`ls -la` |
| `cd` | 場所を移動 | `cd ~`、`cd Desktop` |
| `echo` | 文字を表示 | `echo Hello, World` |
| `cat` | ファイル内容を表示 | （後でファイルを作ってから） |

最後に必ず：

```bash
ls --help
```

→ 大量のオプションが出てくる。**全部覚えなくていい**。「ここを見ればいい」と知ることが大事。

**Windows補足**
- Git Bash 前提なので、Mac と**まったく同じコマンド**が動く
- 補足説明は不要（これが Git Bash 統一のメリット）

**避けるべき脱線**
- PowerShell の独自記法 → 触れない
- パイプ `|` とリダイレクト `>` → 応用編で紹介、ハンズオンでは触れない
- 「引数」と「オプション」の用語の厳密な使い分け → 初心者には「右側は全部追加指示」で十分

**FAQ想定**
- Q「タブで補完できる？」→ A「できます！半角入力中にTabキー、めちゃくちゃ便利。今日中に必ず使ってください」
- Q「上下キーで履歴が出る？」→ A「出ます。↑キーで一個前のコマンドを呼び出せる」
- Q「`-la` と `-l -a` は同じ？」→ A「同じ。短いオプションは繋げて書ける慣習」

**(4) ショートカットキー — 命綱（1分）★**

ハンズオン②の後、必ず触れる：

| キー | 効果 | 重要度 |
|---|---|---|
| `Ctrl + C` | 実行中のコマンドを止める | ★★★ |
| `Ctrl + L` | 画面をクリア | ★★ |
| `Ctrl + R` | 履歴をインクリメンタル検索 | ★★ |
| `Ctrl + A` / `Ctrl + E` | 行頭 / 行末へ | ★ |
| `Ctrl + D` | ターミナルを閉じる | ★ |

実演手順：
1. `sleep 100` を打って実行
2. 動かないように見える状況を見せる
3. `Ctrl + C` で止める → 「これ知らないと、ハマったときにパニックになります」
4. `Ctrl + R` を1回だけ実演（`ls` などで履歴検索デモ）

---

### 3. ファイル・ディレクトリ操作（13分）★ハンズオン③ — 最重要

**話すポイント**
- **カレントディレクトリ** = 「今いる場所」
  - メタファ：あなたの「立ち位置」。すべてのコマンドはここを基準に動く
- **ホームディレクトリ** = 「自分の家」
  - Windowsなら `C:\Users\<あなたの名前>`
  - Macなら `/Users/<あなたの名前>`
- **チルダ `~` の説明（必ず触れる）**
  - 読み方：**「チルダ」**（英語: tilde）
  - 意味：ホームディレクトリの省略記号
  - `cd ~` でどこからでも一発でホームに戻れる
  - `cd ~/Documents` のようにホーム起点の道順も書ける
  - ターミナルのプロンプトに `~` と表示されていたら「今ホームにいる」のサイン
- **絶対パスと相対パス**
  - 絶対パス：`/Users/yuki/Desktop/memo.txt`（住所をフルで書く）
  - 相対パス：`Desktop/memo.txt`（現在地からの道順）
- **`.` と `..`**
  - `.` = カレント（今いるところ）
  - `..` = 一つ上

**ハンズオン③：作業フォルダで CRUD**

```bash
cd ~                          # ホームに移動（cd だけでも可）
mkdir cli-handson             # フォルダを作る
cd cli-handson                # 移動
pwd                           # 今ここを確認
echo "Hello CLI" > memo.txt   # ファイルを1コマンドで作成
ls                            # memo.txt が見えるはず
cat memo.txt                  # 中身を表示
cp memo.txt note.txt          # コピー
mv note.txt log.txt           # 名前変更（または移動）
rm log.txt                    # 削除
ls
```

> リダイレクト `>` は「コマンドの出力をファイルに保存」する記号。
> 講座では「ファイル作成の便利ワザ」として導入のみ（深入りは応用編）。

**重要：`rm -rf` の話（必ずやる、1分）**
- `rm` は**ゴミ箱を経由しない**。即削除
- `rm -rf /` は絶対やってはいけない（PCが死ぬ）
- 「rmする前に `ls` で確認する癖」を強調

**おまけ：`tree` でフォルダ構造を一発で見る（30秒）**
```bash
tree ~/cli-handson
```
- Mac は `brew install tree` で入れる、Git Bash は標準搭載

**GUI⇄CLI の橋渡し：`open .` / `explorer .`（1分）★**
- Mac: `open .` で今のフォルダを Finder で開く
- Windows (Git Bash): `explorer .` で今のフォルダをエクスプローラーで開く
- 「CLIで作業 → GUIで確認」の往復が一瞬になる
- 「CLI と GUI は対立じゃない、使い分けて行き来するのが最強」と伝える

**避けるべき脱線**
- `find` `grep` → 次のセクション4で扱う（ここでは出さない）
- シンボリックリンク → 今日は触れない

**FAQ想定**
- Q「Windowsのフォルダ区切りは `\` じゃない？」→ A「Git Bash では Mac と同じ `/` を使います。今日は `/` で統一」
- Q「日本語ファイル名・スペース入りファイル名は？」→ A「動きます。スペースは `"my file.txt"` のようにクオートで囲む」

---

### 4. 探す：`find` と `grep`（5分）★ハンズオン④ — AIエージェントの心臓部

**話すポイント**
- オープニングで触れた「**Claude Codeが裏側でやっていること**」=「ファイルを探して、中身を読んで、必要なものを見つける」
- これを人間がやるためのコマンドが `find`（ファイル名で探す）と `grep`（中身で探す）
- **この2つができると、CLIの世界が一気に広がる**

**(1) `find` — 名前で探す**
```bash
find . -name "memo.txt"         # カレント以下から memo.txt を探す
find ~ -name "report.txt"       # ホーム以下から report.txt を探す
```

- `.` = カレントから / `~` = ホームから
- `-name` の後ろに**探したいファイル名**
- 出てくるのは **見つかった場所のパスの一覧**
- `ls` との違い：`ls` は「今ここだけ」、`find` は「サブフォルダも全部」

> **注**：ワイルドカード（`*`）は今日はスコープ外。応用編で扱う。
> 今日は「ファイル名をそのまま指定して、サブフォルダ含めて探す」を体感する。

**(2) `grep` — 中身で探す**
```bash
grep "Hello" memo.txt           # memo.txt の中で "Hello" を含む行を表示
grep -r "TODO" .                # カレント以下のファイル全てを再帰検索
grep -i "error" log.txt         # 大文字小文字を無視
grep -n "import" hello.py       # 行番号付きで表示
```

- `-r` （recursive）= フォルダ以下を全部見る
- `-i` （ignore case）= 大文字小文字を区別しない
- `-n` （number）= 行番号を付ける
- オプションは複数組み合わせOK：`grep -rin "todo" .`

**Windows補足**
- Git Bash 前提なので、`find` も `grep` も Mac とまったく同じく動く
- 講座中の追加説明は不要

**ハンズオン④：ファイルを探す・中身を探す**

セクション3で作った `~/cli-handson` の `memo.txt` に加えて、2つ追加：

```bash
cd ~/cli-handson
echo "TODO: 後で書く" > task.txt
echo "Hello again" > greeting.txt

# 名前で探す（フルファイル名でOK。findはサブフォルダも見てくれる）
find . -name "memo.txt"
find . -name "task.txt"

# 中身で探す（具体的なファイル名を指定）
grep "Hello" memo.txt
grep "Hello" greeting.txt
grep -r "TODO" .
```

> **注**：ワイルドカード `*` は今日はスコープ外。find/grep は具体的なファイル名を指定する形で体感する。

**ここで一言**：
> 「今やったこれ、Claude Code が裏で動くたびに何百回もやっています。あなたが今、AIの心臓部と同じことをしました」

**避けるべき脱線**
- ワイルドカード `*` `?` → 今日はスコープ外。応用編で扱う
- 正規表現の話 → 一切触れない
- `find -exec` → 応用編
- `sed` `awk` → 名前だけ紹介、ハンズオンなし
- リダイレクト `>` `>>` → ハンズオン③でファイル作成に「便利ワザ」として導入のみ。挙動の深堀りは応用編

**FAQ想定**
- Q「`find` と `ls` の違いは？」→ A「`ls` は今ここの一覧。`find` はサブフォルダも含めて再帰的に検索」
- Q「（参加者がうっかり PowerShell を開いてしまった場合）`find` `grep` が思った動きをしないんですが」→ A「PowerShell を閉じて、Git Bash を開き直してください。今日は Git Bash 前提です」

---

### 5. 環境変数（4分）★ハンズオン⑤

**話すポイント**

**(1) 環境変数とは（30秒）**
- PC 全体（＝あなたが起動するすべてのプログラム）に共有される「名前 = 値」のメモ
- ターミナル開いた瞬間からセット済み（`$HOME` `$SHELL` `$LANG`）
- **慣習：大文字＋アンダースコアで命名**（`ANTHROPIC_API_KEY`）— Unix 系全体の暗黙ルール

**(2) 環境変数の2種類（30秒）**
- **予約変数**：システムが定義・使う（`PATH` `HOME` `USER` `SHELL` `PWD` `LANG`）
- **ユーザ変数**：自分で作る・入れる（`ANTHROPIC_API_KEY` `OPENAI_API_KEY` `EDITOR` etc.）

**(3) なぜAPIキーを環境変数に入れるのか（1分）★重要**
- **理由1**：コードに直接書くと Git push した瞬間に世界に漏れる → 第三者に悪用 → 多額の課金請求（実話多数）
- **理由2**：環境ごとに違うキー（dev / prod）を切り替えやすい
- **理由3**：コード自体は「キーを知らない」状態で配布できる、チーム共有しやすい
- **AI時代のセキュリティの基本**（Claude / OpenAI / Gemini など全てこの形）

**ハンズオン⑤：`PATH` を覗く**
- Windows（Git Bash）/ Mac 共通：`echo $PATH`
- コロン `:` 区切りでフォルダのリストが出る
- 「ここに含まれているフォルダの中の実行ファイルだけが、名前だけで起動できる」を説明

> 余裕があれば「PowerShell では `$env:PATH` という書き方になる」と1秒だけ触れて、Git Bash で統一する意義を再強調する。

**避けるべき脱線**
- `.bashrc` `.zshrc` `$PROFILE` の編集 → 応用編。今日は「読むだけ」
- `export` vs `setx` の OS 差分の細かい話

**FAQ想定**
- Q「環境変数の追加方法は？」→ A「今日はやりませんが、Windowsはシステム設定 GUI、Macは `.zshrc` に追記。次回」

---

### 6. Claude Code を自分で動かす（9分）★ハンズオン⑥ ★ゴール

**話すポイント**
- 「ここまでの50分の意味」をまず話す：ターミナルを開き、コマンドを打ち、ファイル操作・検索・PATHを覗いた。**これらは全てClaude Codeの裏側でやっていること**
- だから、これから Claude Code を自分で起動して使うとき、何が起きているか**見える**
- ゴール：「**AIに業務を任せられる**」状態に持っていく

**ハンズオン⑥-A：Claude Code を起動する（3分）**
```bash
cd ~/cli-handson
claude
```
- カレントディレクトリの概念がここで活きる：「今いる場所で起動 = ここを見てくれる」
- 起動後、Claude Code のプロンプトが立ち上がる

**ハンズオン⑥-B：自然言語で頼む（5分）**
例として：
> 「このフォルダのファイル一覧を見て、それぞれ何が書いてあるか1行で要約して」

裏でClaude Codeが叩くコマンドを実況：
- `ls` / `find` でファイル一覧
- `cat` で中身を読む
- 自然言語で要約

→ 受講者は**今日学んだコマンドがそのまま動いている**ことを認識できる

**ゴール達成スライド（1分）**
- 6つできるようになった、と振り返る
- 「AI時代のPCリテラシーを獲得した」と宣言

**避けるべき脱線**
- Claude Codeの細かい使い方（プロンプトテクニック等）→ 別講座へ
- API キーの話 → セクション5で軽く触れた程度でOK
- `claude --help` の中身詳細 → 「自分で見てみて」で送り出す

**FAQ想定**
- Q「Claude Code が `claude` で起動しないんですが」→ A「インストールが完了していないか、PATH が通っていない可能性。`claude --version` を確認」
- Q「Claude Code に何でも頼める？」→ A「ほぼ何でも。ただし、機密データを扱う場合は社内ルールを確認」

---

### 7. 次に効くコマンド集（2分）

**目的**：今日の本編で扱えなかったけど、業務で5回に1回出番が来るコマンドを名前だけ紹介。
受講者が後から自習できる足がかりにする。

**話すポイント**
- 「今日のあと、業務で5回に1回くらいの頻度で出番が来ます」
- 1スライドで一覧表示、深入りしない
- 「Claude Code に『これを使って〇〇して』と頼むだけでもOK」

**紹介するコマンド**

| コマンド | できること |
|---|---|
| `curl` | API を叩く / ファイルをダウンロード |
| `head` / `tail` | ファイルの先頭・末尾だけ表示 |
| `wc -l` | 行数カウント |
| `history` / `Ctrl + R` | 過去のコマンド呼び戻し・検索 |
| `tree` | フォルダ構造を図示 |
| `open .` / `explorer .` | 今のフォルダを GUI で開く |
| `zip` / `unzip` | 圧縮・解凍 |
| `diff` | ファイル比較 |

**避けるべき脱線**
- それぞれの使い方を実演しない（時間がない）
- 「次回」「チートシート」で詳しくと予告

---

### 8. 応用デモ（5分・見せるだけ）

**目的**：「今日学んだ基礎の上に、こんな世界がある」を見せて、次の自習のモチベーションにする

**デモ①：100枚の画像を一括リサイズ（30秒）**
- Mac: `sips -Z 800 *.jpg`
- Windows (Git Bash + ImageMagick): `magick mogrify -resize 800x *.jpg`
- 「これGUIでやったら2時間です」
- 講師ノート：ここで初めて `*` が出てくる。**1秒だけ**「これはワイルドカードという便利な記号で、"全部"の意味です。次回詳しくやります」と触れる。深入りしない。

**デモ②：連番ファイルを1コマンドで作る（30秒）**
```bash
for i in {1..100}; do touch "report_$(printf "%03d" $i).txt"; done
```
- 「`report_001.txt` から `report_100.txt` が一瞬で」

**デモ③：シェルスクリプトで毎朝のバックアップ自動化（1.5分）**
- 5行ぐらいの簡単な `.sh` ファイルを見せる
- 「これをタスクスケジューラ / cron に登録すれば毎日自動」

**デモ④：Claude Code で実務を任せる（3.5分）**
- 実際にClaude Codeを起動して、自然言語で業務指示（例：「このフォルダのCSVを集計して」）
- 裏でClaude Codeが `find` `grep` `cat` `python` を組み合わせて動くのを実況
- 「今日学んだCLIの土台が、そのままAIの土台。あなたが分かれば、AIも分かる」

**避けるべき脱線**
- 受講者に「打ってください」と言わない。**見せるだけ**
- 質問は最後にまとめて

---

### 9. クロージング（3分）

**話すポイント**
- 今日のゴール達成のお祝い：プログラムを動かせた
- 次のステップ：
  1. 普段の業務で5回に1回、CLIで解決できないか考えてみる
  2. Claude Code を業務で使ってみる
  3. 困ったら社内Slackの `#ai` で聞く
- 参考資料：README記載のURL集

---

## 3. 事前準備（受講者向け案内に転記する）

**全員**
- ノートPCを持参（業務PC可）
- インターネット接続
- テキストエディタ（メモ帳でOK）
- 講座開始10分前にツールが起動できるか確認

**Windowsユーザー**
- [Git for Windows](https://git-scm.com/download/win) をインストール（Git Bash が同梱される）
- スタートメニュー → `Git Bash` で起動できることを確認
- Python のインストール（事前にこちらが配布する手順書を実施。`Add Python to PATH` 必須）

**Macユーザー**
- ターミナル.app が起動できること（標準搭載）
- Python は標準搭載 or `python3` コマンド確認

---

## 4. 講師の機材・準備

- 画面共有：1920x1080、ターミナルのフォントは18pt以上
- ターミナル背景：白／文字：黒（受講者の目に合わせる）
- 録画：ON（後日アーカイブ用）
- スライドとターミナルの切り替え：`Cmd+Tab` をスムーズに
- 事前デモは必ずもう一度通しで実行確認（特に応用デモ）

---

## 5. 次回以降の応用編トピック（今日は触れない、最後に名前だけ紹介）

- パイプ `|` とリダイレクト `>` `>>` `<`
- ワイルドカード `*` `?` の使いこなし
- `awk` `sed` / 正規表現の本格運用
- `find -exec` でファイル一括処理
- WSL / Docker
- `.bashrc` / `.zshrc` のカスタマイズ
- Git の使い方
- シェルスクリプト本格編
- SSH と公開鍵
